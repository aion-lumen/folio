import {it,expect} from 'vitest';import {appointmentAttention} from './attention.js';
const source={fact_id:'source',domain:'personal',data_class:'appointment',subject:'RAV Beratung',predicate:'scheduled_for',source_ref:'mail:synthetic:1',value_text:'RAV Beratung am 2026-09-24 um 10:00',valid_from:'2026-09-24',status:'candidate'};
it('surfaces a future proposal even when its end time is unknown',()=>{expect(appointmentAttention([source],[],new Date('2026-09-06T12:00:00Z'))).toMatchObject([{id:'source',candidate:true,possible:false}]);});
it('does not confuse another event on the same day with coverage',()=>{expect(appointmentAttention([source],[{id:'other',summary:'Dental visit',start:{dateTime:'2026-09-24T10:00:00+02:00'},end:{dateTime:'2026-09-24T11:00:00+02:00'}}],new Date('2026-09-06'))).toHaveLength(1);});
it('suppresses a matching appointment including the visible 30 minute assumption and excludes past dates',()=>{const events=[{id:'match',summary:'RAV Beratung',start:{dateTime:'2026-09-24T10:00:00+02:00'},end:{dateTime:'2026-09-24T10:30:00+02:00'}}];expect(appointmentAttention([source],events,new Date('2026-09-06'))).toEqual([]);expect(appointmentAttention([source],[],new Date('2026-10-01'))).toEqual([]);});
